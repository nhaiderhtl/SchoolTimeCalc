---
phase: 05-presentation-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/SchoolTimeCalc.Web/Components/Widgets/SubjectBreakdownWidget.razor
  - src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
autonomous: true
requirements: [UI-02, UI-04]
must_haves:
  truths:
    - User can navigate to a detailed breakdown of remaining lessons per subject
    - Dense tabular data is displayed using Expandable Cards (Accordions) on mobile
    - Progress bars visually represent remaining time per subject
  artifacts:
    - path: src/SchoolTimeCalc.Web/Components/Widgets/SubjectBreakdownWidget.razor
      provides: Detailed breakdown of subjects with progress bars and accordions
    - path: src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
      provides: Main dashboard including the subject breakdown
  key_links:
    - from: src/SchoolTimeCalc.Web/Components/Widgets/SubjectBreakdownWidget.razor
      to: src/SchoolTimeCalc.Web/Services/ICalculationService.cs
      via: Dependency Injection
      pattern: "@inject ICalculationService"
---

<objective>
Create a Subject Breakdown widget using progress bars and expandable cards (accordions) for mobile devices, and integrate it into the Dashboard.

Purpose: Fulfill UI-02 and UI-04 per D-02 and D-03 by providing detailed per-subject lessons remaining in an accessible format.
Output: SubjectBreakdownWidget component and updated Dashboard.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-presentation-dashboard/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SubjectBreakdownWidget Component</name>
  <files>src/SchoolTimeCalc.Web/Components/Widgets/SubjectBreakdownWidget.razor</files>
  <read_first>
    - src/SchoolTimeCalc.Web/Components/Widgets/SubjectBreakdownWidget.razor
    - src/SchoolTimeCalc.Web/Services/ICalculationService.cs
  </read_first>
  <action>
    Create `SubjectBreakdownWidget.razor`. Inject `ICalculationService` to get per-subject data.
    Implement expandable cards (accordions) for each subject using Tailwind.
    Inside the header of each card, display the subject name and a progress bar showing remaining lessons vs total lessons.
    Inside the expanded body, show additional details (e.g., canceled lessons if available).
  </action>
  <verify>
    <automated>grep -q "progress" src/SchoolTimeCalc.Web/Components/Widgets/SubjectBreakdownWidget.razor || exit 0; echo "Pass"</automated>
  </verify>
  <acceptance_criteria>
    - Component exists at `src/SchoolTimeCalc.Web/Components/Widgets/SubjectBreakdownWidget.razor`
    - Contains progress bar elements
    - Contains accordion/expandable markup
  </acceptance_criteria>
  <done>Subject breakdown widget is created with progress bars and accordions</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SubjectBreakdownWidget into Dashboard</name>
  <files>src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor</files>
  <read_first>
    - src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
  </read_first>
  <action>
    Update `Dashboard.razor` to include `<SubjectBreakdownWidget />` below the `MacroProgressWidget`.
    Ensure proper spacing and responsive layout so that the widgets stack correctly on mobile devices.
  </action>
  <verify>
    <automated>grep -q "SubjectBreakdownWidget" src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor</automated>
  </verify>
  <acceptance_criteria>
    - `SubjectBreakdownWidget` is included in `Dashboard.razor`
  </acceptance_criteria>
  <done>Subject breakdown is visible on the main dashboard</done>
</task>

</tasks>

<verification>
Ensure the subject breakdown correctly renders data from the calculation service with progress bars and expandability.
</verification>

<success_criteria>
Users can view and interact with the subject breakdown on the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-dashboard/05-02-SUMMARY.md`
</output>