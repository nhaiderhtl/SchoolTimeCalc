---
phase: 05-presentation-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
  - src/SchoolTimeCalc.Web/Components/Widgets/MacroProgressWidget.razor
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - User can view high-level macro progress of remaining days and hours
    - Progress is displayed on a mobile-friendly dashboard
  artifacts:
    - path: src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
      provides: Main dashboard layout
    - path: src/SchoolTimeCalc.Web/Components/Widgets/MacroProgressWidget.razor
      provides: Widget for days and hours remaining
  key_links:
    - from: src/SchoolTimeCalc.Web/Components/Widgets/MacroProgressWidget.razor
      to: src/SchoolTimeCalc.Web/Services/ICalculationService.cs
      via: Dependency Injection
      pattern: "@inject ICalculationService"
---

<objective>
Create the main Dashboard page and a widget to display high-level macro progress (remaining days and hours).

Purpose: Fulfill requirement UI-01 by giving users a quick, mobile-friendly overview of their remaining school time.
Output: A new Dashboard page and the MacroProgressWidget component.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-presentation-dashboard/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MacroProgressWidget Component</name>
  <files>src/SchoolTimeCalc.Web/Components/Widgets/MacroProgressWidget.razor</files>
  <read_first>
    - src/SchoolTimeCalc.Web/Components/Widgets/MacroProgressWidget.razor
    - src/SchoolTimeCalc.Web/Services/ICalculationService.cs
  </read_first>
  <action>
    Create a new Blazor component `MacroProgressWidget.razor` that injects `ICalculationService`. 
    It should display the total remaining days and total remaining hours using Tailwind CSS for styling.
    Ensure the layout is responsive and looks good on mobile devices (e.g., using flex or grid with appropriate padding and text sizes).
  </action>
  <verify>
    <automated>grep -q "ICalculationService" src/SchoolTimeCalc.Web/Components/Widgets/MacroProgressWidget.razor</automated>
  </verify>
  <acceptance_criteria>
    - Component exists at `src/SchoolTimeCalc.Web/Components/Widgets/MacroProgressWidget.razor`
    - Injects `ICalculationService`
    - Contains Tailwind CSS classes for responsive design
  </acceptance_criteria>
  <done>Widget is created and uses ICalculationService</done>
</task>

<task type="auto">
  <name>Task 2: Build Dashboard Page</name>
  <files>src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor</files>
  <read_first>
    - src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
  </read_first>
  <action>
    Create the `Dashboard.razor` page mapped to the `/dashboard` route.
    Add a page title and include the `MacroProgressWidget` at the top of the page.
    Use Tailwind classes to create a responsive container for the dashboard content.
  </action>
  <verify>
    <automated>grep -q "@page \"/dashboard\"" src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor</automated>
  </verify>
  <acceptance_criteria>
    - Component exists at `src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor`
    - Contains `@page "/dashboard"`
    - Includes `<MacroProgressWidget />`
  </acceptance_criteria>
  <done>Dashboard page is created and includes the progress widget</done>
</task>

</tasks>

<verification>
Ensure the Dashboard page compiles and renders the MacroProgressWidget which fetches data from ICalculationService.
</verification>

<success_criteria>
Dashboard route is accessible and displays remaining days and hours accurately.
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-dashboard/05-01-SUMMARY.md`
</output>