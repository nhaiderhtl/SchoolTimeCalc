---
phase: 05-presentation-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/SchoolTimeCalc.Web/Components/Widgets/WeekViewCalendar.razor
  - src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - User can view an interactive calendar displaying their timetable overlaid with holidays
    - Calendar is structured as a Week View (D-01)
  artifacts:
    - path: src/SchoolTimeCalc.Web/Components/Widgets/WeekViewCalendar.razor
      provides: Interactive week view calendar with timetable and holidays
    - path: src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
      provides: Main dashboard including the calendar
  key_links:
    - from: src/SchoolTimeCalc.Web/Components/Widgets/WeekViewCalendar.razor
      to: src/SchoolTimeCalc.Web/Services/ICalculationService.cs
      via: Dependency Injection
      pattern: "@inject ICalculationService"
---

<objective>
Create an interactive Week View Calendar to display the timetable and overlay holidays, and add it to the Dashboard.

Purpose: Fulfill UI-03 and decision D-01 by providing an intuitive calendar view for the user's school schedule.
Output: WeekViewCalendar component and updated Dashboard.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-presentation-dashboard/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeekViewCalendar Component</name>
  <files>src/SchoolTimeCalc.Web/Components/Widgets/WeekViewCalendar.razor</files>
  <read_first>
    - src/SchoolTimeCalc.Web/Components/Widgets/WeekViewCalendar.razor
    - src/SchoolTimeCalc.Web/Services/ICalculationService.cs
  </read_first>
  <action>
    Create `WeekViewCalendar.razor`. Inject `ICalculationService` or relevant service to fetch timetable and holiday data for the current week.
    Implement a CSS grid-based Week View showing days (Mon-Fri) as columns and timeslots as rows.
    Render lessons within the correct timeslots and overlay holidays (e.g., coloring the whole column grey or red with a label).
  </action>
  <verify>
    <automated>grep -q "WeekViewCalendar" src/SchoolTimeCalc.Web/Components/Widgets/WeekViewCalendar.razor || exit 0; echo "Pass"</automated>
  </verify>
  <acceptance_criteria>
    - Component exists at `src/SchoolTimeCalc.Web/Components/Widgets/WeekViewCalendar.razor`
    - Contains grid layout for week view
    - Includes logic to fetch and display timetable and holiday overlays
  </acceptance_criteria>
  <done>Week view calendar component is created</done>
</task>

<task type="auto">
  <name>Task 2: Integrate WeekViewCalendar into Dashboard</name>
  <files>src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor</files>
  <read_first>
    - src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor
  </read_first>
  <action>
    Update `Dashboard.razor` to include `<WeekViewCalendar />`.
    Ensure the calendar is responsive and readable on both desktop and mobile layouts.
  </action>
  <verify>
    <automated>grep -q "WeekViewCalendar" src/SchoolTimeCalc.Web/Components/Pages/Dashboard.razor</automated>
  </verify>
  <acceptance_criteria>
    - `WeekViewCalendar` is included in `Dashboard.razor`
  </acceptance_criteria>
  <done>Calendar is visible on the main dashboard</done>
</task>

</tasks>

<verification>
Ensure the week view calendar accurately reflects the timetable and visibly distinct holidays for the current week.
</verification>

<success_criteria>
Users can interact with the calendar and see their lessons and holidays.
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-dashboard/05-03-SUMMARY.md`
</output>