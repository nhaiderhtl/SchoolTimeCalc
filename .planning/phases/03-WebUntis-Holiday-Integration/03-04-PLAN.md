---
phase: 03-WebUntis-Holiday-Integration
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - SchoolTimeCalc/Services/HolidaySyncBackgroundService.cs
  - SchoolTimeCalc/Services/SchoolHolidayService.cs
  - SchoolTimeCalc/Services/NationalHolidayService.cs
  - SchoolTimeCalc/Services/WebUntisHolidaySyncService.cs
autonomous: true
requirements: ["SYNC-03", "SYNC-04"]
gap_closure: true
must_haves:
  truths:
    - "Background service synchronizes holidays periodically for active users using real credentials"
    - "Austrian public bank holidays and school holidays are fetched dynamically via an API and integrated into the holiday list"
  artifacts:
    - path: "SchoolTimeCalc/Services/HolidaySyncBackgroundService.cs"
      provides: "Background sync with real credentials"
    - path: "SchoolTimeCalc/Services/SchoolHolidayService.cs"
      provides: "Real API call for school holidays"
    - path: "SchoolTimeCalc/Services/WebUntisHolidaySyncService.cs"
      provides: "Integration of national and state holidays"
  key_links:
    - from: "SchoolTimeCalc/Services/HolidaySyncBackgroundService.cs"
      to: "SchoolTimeCalc/Data/ApplicationDbContext"
      via: "Fetches user credentials for background job"
---

<objective>
Fix background service authentication and wire up orphaned holiday services to complete dynamic holiday synchronization.

Purpose: Close gaps identified in phase verification where the background sync was a stub failing in production due to hardcoded credentials, and national/school holiday services were orphaned stubs.
Output: Working background sync pulling real user credentials, and fully wired API calls for Austrian bank and school holidays.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-WebUntis-Holiday-Integration/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secure Background Sync Authentication</name>
  <read_first>SchoolTimeCalc/Services/HolidaySyncBackgroundService.cs</read_first>
  <files>SchoolTimeCalc/Services/HolidaySyncBackgroundService.cs</files>
  <action>
    - Remove hardcoded "placeholder_password" and server strings in `SyncAllUsersHolidaysAsync`.
    - Update the service to query the database (e.g., `ApplicationDbContext`) for valid, active user credentials (username, encrypted/stored password, server, school).
    - Iterate through active users, authenticating with their real WebUntis credentials, and triggering the holiday sync.
    - Handle cases where user credentials might be missing or invalid gracefully without crashing the background worker.
    
    Gap reason: The background sync service uses a hardcoded placeholder password and server, making it a functional stub that will fail in production.
  </action>
  <verify>
    <automated>grep -L "placeholder_password" SchoolTimeCalc/Services/HolidaySyncBackgroundService.cs</automated>
  </verify>
  <acceptance_criteria>
    - SchoolTimeCalc/Services/HolidaySyncBackgroundService.cs does not contain "placeholder_password"
    - Service queries DbContext for user credentials before attempting sync
  </acceptance_criteria>
  <done>Background service successfully loads credentials dynamically instead of using hardcoded stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement and Wire Orphaned Holiday Services</name>
  <read_first>SchoolTimeCalc/Services/SchoolHolidayService.cs, SchoolTimeCalc/Services/WebUntisHolidaySyncService.cs</read_first>
  <files>SchoolTimeCalc/Services/SchoolHolidayService.cs, SchoolTimeCalc/Services/WebUntisHolidaySyncService.cs, SchoolTimeCalc/Services/NationalHolidayService.cs</files>
  <action>
    - Update `SchoolTimeCalc/Services/SchoolHolidayService.cs` to remove the hardcoded static array of dates and implement a real API call (e.g., to data.gv.at or another API as designed) for Austrian state school holidays.
    - Update `SchoolTimeCalc/Services/WebUntisHolidaySyncService.cs` to inject and invoke `NationalHolidayService` and `SchoolHolidayService` during the main synchronization process.
    - Merge the fetched bank holidays and school holidays with the holidays fetched directly from WebUntis, persisting them all to the database.
    - Ensure `NationalHolidayService` (which uses Nager.Date) is correctly utilized to satisfy SYNC-03.
    
    Gap reason: NationalHolidayService is orphaned. SchoolHolidayService is both a stub (returns hardcoded dates) and orphaned.
  </action>
  <verify>
    <automated>grep "NationalHolidayService" SchoolTimeCalc/Services/WebUntisHolidaySyncService.cs</automated>
  </verify>
  <acceptance_criteria>
    - SchoolTimeCalc/Services/SchoolHolidayService.cs no longer returns a hardcoded list of dates
    - SchoolTimeCalc/Services/WebUntisHolidaySyncService.cs injects and calls INationalHolidayService and ISchoolHolidayService
  </acceptance_criteria>
  <done>Austrian public bank holidays and school holidays are fetched dynamically via an API and integrated into the overall holiday sync pipeline.</done>
</task>

</tasks>

<verification>
- Verify that `HolidaySyncBackgroundService` correctly retrieves credentials from the database.
- Verify that manual or automatic sync operations execute code in `NationalHolidayService` and `SchoolHolidayService`.
</verification>

<success_criteria>
- Background service no longer fails due to stubbed credentials.
- Orphaned holiday services are integrated into the main holiday sync workflow.
- Austrian specific holidays (SYNC-03, SYNC-04) are fully functional.
</success_criteria>

<output>
After completion, create `.planning/phases/03-WebUntis-Holiday-Integration/03-04-SUMMARY.md`
</output>