---
phase: 03-WebUntis-Holiday-Integration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: ["src/SchoolTimeCalc.Core/Interfaces/IWebUntisService.cs", "src/SchoolTimeCalc.Infrastructure/Services/WebUntisService.cs", "src/SchoolTimeCalc.Infrastructure/Services/WebUntisHolidaySyncService.cs", "src/SchoolTimeCalc.Infrastructure/Services/SchoolHolidayService.cs", "src/SchoolTimeCalc.Web/Components/Pages/Settings.razor"]
autonomous: true
requirements: ["SYNC-03", "SYNC-04"]

must_haves:
  truths:
    - "System persists WebUntis credentials properly"
    - "Holiday sync fetches alongside timetable sync"
    - "Subdivision mapping generates correct Austrian state codes"
    - "Settings UI properly displays sync failures"
  artifacts:
    - path: "src/SchoolTimeCalc.Infrastructure/Services/WebUntisService.cs"
      provides: "Timetable and Holiday Sync"
    - path: "src/SchoolTimeCalc.Infrastructure/Services/SchoolHolidayService.cs"
      provides: "Correct Austrian State code mapping"
  key_links:
    - from: "src/SchoolTimeCalc.Infrastructure/Services/WebUntisService.cs"
      to: "WebUntisHolidaySyncService"
      via: "DI call"
      pattern: "SyncHolidaysAsync"
---

<objective>
Fix core integration issues identified in Phase 03 reviews: credentials persistence, holiday sync triggering, subdivision mapping, and UI error handling.

Purpose: Address Reviewer concerns 1, 2, 3, and 4
Output: Working, integrated holiday sync using correct state mapping
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-WebUntis-Holiday-Integration/03-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Credentials Persistence and Integration</name>
  <files>src/SchoolTimeCalc.Infrastructure/Services/WebUntisService.cs</files>
  <read_first>src/SchoolTimeCalc.Infrastructure/Services/WebUntisService.cs</read_first>
  <action>Modify `WebUntisService.AuthenticateAndSyncAsync` to persist `Server` and `Username` along with the password, so background sync works correctly. Also, call `WebUntisHolidaySyncService.SyncHolidaysAsync` directly from `AuthenticateAndSyncAsync` to satisfy the "alongside timetables" requirement.</action>
  <verify>
    <automated>grep -q "SyncHolidaysAsync" src/SchoolTimeCalc.Infrastructure/Services/WebUntisService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `WebUntisService.cs` contains `SyncHolidaysAsync`
    - `WebUntisService.cs` contains logic to save `Server` and `Username` to user profile
  </acceptance_criteria>
  <done>Credentials persisted and SyncHolidaysAsync called alongside timetables</done>
</task>

<task type="auto">
  <name>Task 2: Fix Subdivision Mapping and UI Error Handling</name>
  <files>src/SchoolTimeCalc.Infrastructure/Services/SchoolHolidayService.cs, src/SchoolTimeCalc.Web/Components/Pages/Settings.razor</files>
  <read_first>src/SchoolTimeCalc.Infrastructure/Services/SchoolHolidayService.cs, src/SchoolTimeCalc.Web/Components/Pages/Settings.razor</read_first>
  <action>In `SchoolHolidayService.cs`, replace the broken substring mapping with a correct dictionary mapping for Austrian states (e.g., Wien -> AT-9, Niederösterreich -> AT-3, etc.). In `Settings.razor` and `WebUntisHolidaySyncService`, ensure exceptions bubble up or return a failure status so the UI shows an error message instead of false success.</action>
  <verify>
    <automated>grep -q "AT-9" src/SchoolTimeCalc.Infrastructure/Services/SchoolHolidayService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `SchoolHolidayService.cs` contains `AT-9`
    - `Settings.razor` contains failure state rendering logic
  </acceptance_criteria>
  <done>Austrian states mapped correctly and errors shown in UI</done>
</task>

</tasks>

<verification>
Ensure background sync runs without crashing due to missing credentials, and the correct holiday data is fetched based on the valid state code.
</verification>

<success_criteria>
- State mapping is correct
- Credentials are fully persisted
- UI shows correct sync status
- Sync happens alongside timetables
</success_criteria>

<output>
After completion, create `.planning/phases/03-WebUntis-Holiday-Integration/03-05-SUMMARY.md`
</output>
