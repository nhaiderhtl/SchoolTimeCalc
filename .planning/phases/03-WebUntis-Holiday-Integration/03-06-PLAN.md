---
phase: 03-WebUntis-Holiday-Integration
plan: 06
type: execute
wave: 2
depends_on: ["03-05"]
files_modified: ["src/SchoolTimeCalc.Core/Services/CalculationEngine.cs", "tests/SchoolTimeCalc.Tests/Services/HolidaySyncServiceTests.cs", "src/SchoolTimeCalc.Web/Components/Pages/Home.razor"]
autonomous: true
requirements: ["SYNC-03", "SYNC-04"]

must_haves:
  truths:
    - "System calculates total school days excluding holidays"
    - "Test coverage replaces placeholders with real tests"
  artifacts:
    - path: "src/SchoolTimeCalc.Core/Services/CalculationEngine.cs"
      provides: "Holiday-integrated calculations"
    - path: "tests/SchoolTimeCalc.Tests/Services/HolidaySyncServiceTests.cs"
      provides: "Real service and integration tests"
  key_links:
    - from: "src/SchoolTimeCalc.Core/Services/CalculationEngine.cs"
      to: "Holiday"
      via: "EF context or cached list"
      pattern: "Holidays"
---

<objective>
Integrate cached holidays into calculation logic and replace placeholder tests with real assertions.

Purpose: Address Reviewer concerns 5 and 6
Output: Working calculation logic with tests
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-WebUntis-Holiday-Integration/03-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Holidays into CalculationEngine</name>
  <files>src/SchoolTimeCalc.Core/Services/CalculationEngine.cs, src/SchoolTimeCalc.Web/Components/Pages/Home.razor</files>
  <read_first>src/SchoolTimeCalc.Core/Services/CalculationEngine.cs</read_first>
  <action>In `CalculationEngine.cs`, modify the logic that computes remaining school days to load the user's `Holidays` from the database. Iterate over the remaining days and exclude any day that falls within a `Holiday`'s `StartDate` and `EndDate`. Show this data loosely on `Home.razor` to satisfy criterion 2.</action>
  <verify>
    <automated>grep -q "Holiday" src/SchoolTimeCalc.Core/Services/CalculationEngine.cs</automated>
  </verify>
  <acceptance_criteria>
    - `CalculationEngine.cs` uses `Holidays` list
    - `Home.razor` displays "Days Remaining (excluding holidays)"
  </acceptance_criteria>
  <done>Calculations use holiday list to subtract days</done>
</task>

<task type="auto">
  <name>Task 2: Replace Placeholder Tests</name>
  <files>tests/SchoolTimeCalc.Tests/Services/HolidaySyncServiceTests.cs</files>
  <read_first>tests/SchoolTimeCalc.Tests/Services/HolidaySyncServiceTests.cs</read_first>
  <action>In `HolidaySyncServiceTests.cs`, remove `Assert.True(true)` placeholders. Write tests that instantiate the real `WebUntisHolidaySyncService` with mock dependencies (e.g., `Mock<INationalHolidayService>`), and assert that holidays are properly fetched and saved to the database. Also write a test for `SchoolHolidayService` state mapping logic.</action>
  <verify>
    <automated>grep -qv "Assert.True(true)" tests/SchoolTimeCalc.Tests/Services/HolidaySyncServiceTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - `HolidaySyncServiceTests.cs` has real assertions using Moq
    - Placeholder tests are removed
  </acceptance_criteria>
  <done>All tests run successfully without placeholders</done>
</task>

</tasks>

<verification>
Ensure all newly written tests pass and the home page correctly subtracts the holiday durations from the total remaining days.
</verification>

<success_criteria>
- Calculation engine correctly subtracts holidays
- Tests cover the actual service behavior and failure states
</success_criteria>

<output>
After completion, create `.planning/phases/03-WebUntis-Holiday-Integration/03-06-SUMMARY.md`
</output>
