---
phase: 02-webuntis-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SchoolTimeCalc/SchoolTimeCalc.csproj
  - SchoolTimeCalc/Models/WebUntisData.cs
  - SchoolTimeCalc/Models/ApplicationUser.cs
  - SchoolTimeCalc/Data/ApplicationDbContext.cs
  - SchoolTimeCalc/Services/UntisRpcModels.cs
  - SchoolTimeCalc/Services/IWebUntisClient.cs
  - SchoolTimeCalc/Program.cs
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "Database schema can store raw WebUntis JSON responses"
    - "Refit interface accurately defines the WebUntis JSON-RPC endpoints"
    - "Polly resilience policies are configured for WebUntis API calls"
  artifacts:
    - path: "SchoolTimeCalc/Models/WebUntisData.cs"
      provides: "EF Core entity for storing JSONB Untis data"
    - path: "SchoolTimeCalc/Services/IWebUntisClient.cs"
      provides: "Refit client interface"
  key_links:
    - from: "SchoolTimeCalc/Program.cs"
      to: "SchoolTimeCalc/Services/IWebUntisClient.cs"
      via: "AddRefitClient registration"
---

<objective>
Configure the database schema and API client contracts for WebUntis integration.

Purpose: Establishes the foundation for communicating with the WebUntis JSON-RPC API and storing its complex nested data securely in PostgreSQL using JSONB columns, following decision D-03.
Output: EF Core models updated, Refit client interface defined, and DI container configured with Polly retries.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webuntis-integration/02-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
From SchoolTimeCalc/Models/ApplicationUser.cs:
```csharp
namespace SchoolTimeCalc.Models
{
    public class ApplicationUser
    {
        public int Id { get; set; }
        public string Username { get; set; } = string.Empty;
        public string? Bundesland { get; set; }
        // We will add a one-to-one relationship to WebUntisData here
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Packages and Database Models</name>
  <files>SchoolTimeCalc/SchoolTimeCalc.csproj, SchoolTimeCalc/Models/WebUntisData.cs, SchoolTimeCalc/Models/ApplicationUser.cs, SchoolTimeCalc/Data/ApplicationDbContext.cs</files>
  <action>
    - Use dotnet add package to install `Refit.HttpClientFactory` and `Microsoft.Extensions.Http.Polly` (v8+).
    - Create `WebUntisData.cs` with properties: Id (int), ApplicationUserId (int), SchoolName (string), and `JsonDocument` or `string` properties for `SubjectsJson`, `TeachersJson`, `RoomsJson`, `LessonsJson`. Configure them to map to JSONB in EF Core if using string or JsonDocument.
    - Update `ApplicationUser` to have a navigation property to `WebUntisData`.
    - Update `ApplicationDbContext` to include `DbSet<WebUntisData>` and configure the one-to-one relationship and column types (`HasColumnType("jsonb")`).
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Models compile and packages are successfully installed.</done>
</task>

<task type="auto">
  <name>Task 2: Define Refit Client and DTOs</name>
  <files>SchoolTimeCalc/Services/UntisRpcModels.cs, SchoolTimeCalc/Services/IWebUntisClient.cs</files>
  <action>
    - Create `UntisRpcModels.cs` defining basic JSON-RPC 2.0 request/response structures. Include `UntisAuthRequest`, `UntisAuthResponse` (contains sessionId), and a generic `UntisRpcResponse<T>`.
    - Create `IWebUntisClient.cs` interface using Refit attributes. Map a POST method for the JSON-RPC endpoint. (e.g. `[Post("/WebUntis/jsonrpc.do")] Task<UntisAuthResponse> AuthenticateAsync([Body] UntisAuthRequest request, [Query] string school);`)
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Refit interface and DTOs are defined with correct JSON-RPC structures.</done>
</task>

<task type="auto">
  <name>Task 3: Configure DI and Polly Policies</name>
  <files>SchoolTimeCalc/Program.cs</files>
  <action>
    - In `Program.cs`, add Polly retry policies using `HttpPolicyExtensions.HandleTransientHttpError().WaitAndRetryAsync(...)`.
    - Register the Refit client: `builder.Services.AddRefitClient<IWebUntisClient>().ConfigureHttpClient(c => c.BaseAddress = new Uri("https://demo.webuntis.com")).AddPolicyHandler(...)`. (The base address can be a placeholder or loaded from config).
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Program.cs successfully compiles with Refit and Polly configured.</done>
</task>

</tasks>

<verification>
Ensure the project compiles with all new dependencies, database context is valid, and the Refit client is correctly registered in the dependency injection container.
</verification>

<success_criteria>
- Build passes without errors.
- Packages for Refit and Polly are present in the csproj.
- EF Core context handles WebUntisData with JSONB columns.
</success_criteria>

<output>
After completion, create `.planning/phases/02-webuntis-integration/02-01-SUMMARY.md`
</output>
