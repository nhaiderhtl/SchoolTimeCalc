---
phase: 02-webuntis-integration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - SchoolTimeCalc/Services/WebUntisService.cs
  - SchoolTimeCalc/Program.cs
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "System fetches and stores base timetable data as JSON in the database"
    - "Service can orchestration login, fetch, and logout flows with WebUntis"
  artifacts:
    - path: "SchoolTimeCalc/Services/WebUntisService.cs"
      provides: "Orchestration logic"
  key_links:
    - from: "SchoolTimeCalc/Services/WebUntisService.cs"
      to: "SchoolTimeCalc/Services/IWebUntisClient.cs"
      via: "DI Constructor injection"
---

<objective>
Implement orchestration logic to synchronize WebUntis timetables to the local PostgreSQL database.

Purpose: Automates the sequence of logging in, retrieving data (subjects, teachers, lessons), storing it via EF Core JSONB, and safely logging out, enforcing the "On-Demand" (D-02) and "Zero Knowledge" (D-01) decisions.
Output: A scoped `WebUntisService` ready to be called by the UI.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webuntis-integration/02-CONTEXT.md

<interfaces>
From 02-01 (created previously):
```csharp
public interface IWebUntisClient { ... }
public class WebUntisData { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Orchestration Logic</name>
  <files>SchoolTimeCalc/Services/WebUntisService.cs</files>
  <action>
    - Create a scoped `WebUntisService` receiving `IWebUntisClient`, `ApplicationDbContext`, and `MockAuthService` or a simulated current-user accessor.
    - Implement `public async Task&lt;bool&gt; AuthenticateAndSyncAsync(string server, string school, string username, string password)`.
    - Flow: 1. Update HttpClient BaseAddress (if needed, or pass full url) 2. Call `AuthenticateAsync` 3. Check for valid sessionId 4. Fetch `getSubjects`, `getTeachers`, `getRooms`, `getTimetable` (base logic - placeholders or actual calls using the session ID in a Cookie or request). 5. Save/Update `WebUntisData` in `ApplicationDbContext` for the current user. 6. Call `LogoutAsync` and return true/false on success.
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Service implementation correctly wraps the sequence of Refit calls and EF Core saves.</done>
</task>

<task type="auto">
  <name>Task 2: Service Registration</name>
  <files>SchoolTimeCalc/Program.cs</files>
  <action>
    - Register `WebUntisService` as a Scoped service in `Program.cs`.
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Service is registered successfully and application builds.</done>
</task>

</tasks>

<verification>
Service successfully compiles and all database saves correctly reference the JSON properties defined in Plan 01.
</verification>

<success_criteria>
- `WebUntisService` encapsulates all interactions with Untis.
- No passwords or session tokens are permanently stored in the EF Core context.
</success_criteria>

<output>
After completion, create `.planning/phases/02-webuntis-integration/02-02-SUMMARY.md`
</output>
