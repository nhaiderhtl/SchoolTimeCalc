---
phase: 02-webuntis-integration
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - SchoolTimeCalc/Components/Pages/WebUntisLogin.razor
  - SchoolTimeCalc/Components/Pages/Home.razor
autonomous: false
requirements: [SYNC-01]
must_haves:
  truths:
    - "User can enter WebUntis credentials in a login form"
    - "User sees a success state when their timetable is successfully synced"
  artifacts:
    - path: "SchoolTimeCalc/Components/Pages/WebUntisLogin.razor"
      provides: "UI form for authentication"
  key_links:
    - from: "SchoolTimeCalc/Components/Pages/WebUntisLogin.razor"
      to: "SchoolTimeCalc/Services/WebUntisService.cs"
      via: "Dependency injection"
---

<objective>
Implement the WebUntis login form UI to collect credentials and display sync progress.

Purpose: Connects the `WebUntisService` orchestration to an interactive user interface, allowing users to authenticate their WebUntis account and sync their timetable directly.
Output: A styled Blazor page and a conditionally updated Home dashboard.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webuntis-integration/02-CONTEXT.md

<interfaces>
From 02-02 (created previously):
```csharp
public class WebUntisService
{
    public Task<bool> AuthenticateAndSyncAsync(string server, string school, string username, string password);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Login Component UI</name>
  <files>SchoolTimeCalc/Components/Pages/WebUntisLogin.razor</files>
  <action>
    - Create a page at `@page "/webuntis-login"`.
    - Create a form with `Server` (URL/domain), `SchoolName`, `Username`, and `Password` input fields.
    - Add a Submit button that is disabled when `isSyncing` is true.
    - Inject `WebUntisService` and call `AuthenticateAndSyncAsync`.
    - Handle loading spinners and success/error messages with Tailwind classes (similar to Settings.razor styles).
    - If success, redirect to `/` (Home).
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Component logic correctly handles submit, loading state, and calls service.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Login Flow into Home</name>
  <files>SchoolTimeCalc/Components/Pages/Home.razor</files>
  <action>
    - Update `Home.razor` to check the `ApplicationDbContext` for the current user (via `MockAuthService`'s returned user ID).
    - If the user has a linked `WebUntisData` record, show "Timetable Synced Successfully" and a small summary of JSON payload length or properties.
    - If not, display a call-to-action button or card: "Connect WebUntis Account" redirecting to `/webuntis-login`.
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Home page correctly queries DB and routes unlinked users to login.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Integration</name>
  <files>SchoolTimeCalc/Components/Pages/WebUntisLogin.razor, SchoolTimeCalc/Components/Pages/Home.razor</files>
  <action>Wait for the user to confirm the login flow works correctly.</action>
  <verify>
    <automated>Human tests the login UI</automated>
  </verify>
  <done>User types approved</done>
  <what-built>WebUntis Login Form and integration</what-built>
  <how-to-verify>
    1. Start the application (`dotnet run`)
    2. Go to the Home page and click "Connect WebUntis Account"
    3. Enter mock or real WebUntis credentials
    4. Verify loading states and that the JSON data is fetched and stored correctly in the Postgres DB
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Ensure the form compiles, styles apply, and dependency injection works for the DbContext and WebUntisService. Check that error states don't leak exceptions to the user UI.
</verification>

<success_criteria>
- The UI properly handles the input of Untis parameters.
- Valid credentials lead to a sync, invalid credentials show a clean error message.
- Navigation correctly directs the user based on sync status.
</success_criteria>

<output>
After completion, create `.planning/phases/02-webuntis-integration/02-03-SUMMARY.md`
</output>
