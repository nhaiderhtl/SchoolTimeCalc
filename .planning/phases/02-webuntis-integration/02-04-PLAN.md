---
phase: 02-webuntis-integration
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - SchoolTimeCalc/Services/IWebUntisClient.cs
  - SchoolTimeCalc/Services/WebUntisService.cs
autonomous: true
gap_closure: true
requirements: [SYNC-02]

must_haves:
  truths:
    - "System fetches and stores base timetable data as JSON in the database"
    - "Service can orchestration login, fetch, and logout flows with WebUntis"
    - "Refit interface accurately defines the WebUntis JSON-RPC endpoints"
  artifacts:
    - path: "SchoolTimeCalc/Services/IWebUntisClient.cs"
      provides: "Full WebUntis JSON-RPC endpoints"
    - path: "SchoolTimeCalc/Services/WebUntisService.cs"
      provides: "Complete data fetching and session cleanup"
  key_links:
    - from: "SchoolTimeCalc/Services/WebUntisService.cs"
      to: "SchoolTimeCalc/Services/IWebUntisClient.cs"
      via: "Method calls for data and logout"
---

<objective>
Close functionality gaps identified during verification. Specifically: implement the missing Refit endpoints for WebUntis API communication, replace hardcoded data arrays with actual API calls to fetch school data, and ensure sessions are securely terminated via a logout call in a finally block.

Purpose: Completes the integration with WebUntis so actual timetable data can be persisted locally.
Output: Fully operational WebUntis data fetch and save process.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webuntis-integration/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete WebUntis API Endpoints</name>
  <files>SchoolTimeCalc/Services/IWebUntisClient.cs</files>
  <action>
    Add Refit endpoint definitions to `IWebUntisClient` for fetching data and logging out.
    
    Add methods for `GetSubjectsAsync`, `GetTeachersAsync`, `GetRoomsAsync`, `GetTimetableAsync`, and `LogoutAsync`.
    Each should accept a `[Body] UntisRpcRequest request`, `[Query] string school`, and `[Header("Cookie")] string jsessionId`.
    Return `Task<UntisRpcResponse<System.Text.Json.JsonElement>>` for the data calls to allow easy raw JSON string serialization, and `Task` or `Task<UntisRpcResponse<object>>` for logout.
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Interface `IWebUntisClient` contains endpoints for all required WebUntis JSON-RPC actions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Data Fetch and Logout Orchestration</name>
  <files>SchoolTimeCalc/Services/WebUntisService.cs</files>
  <action>
    Update `AuthenticateAndSyncAsync` to replace the hardcoded "[]" placeholders with real API calls.
    
    1. Send a request for "getSubjects", "getTeachers", "getRooms", and "getTimetable".
    2. The session ID (`response.Result.SessionId`) MUST be passed as a cookie string formatted as `$"JSESSIONID={sessionId}"` to the new client methods.
    3. Serialize the `JsonElement` results to string (using `System.Text.Json.JsonSerializer.Serialize`) and assign them to `subjectsJson`, `teachersJson`, `roomsJson`, and `lessonsJson`.
    4. For "getTimetable", construct an `options` parameter with `id` (use `response.Result.PersonId` if person type is 5/Student, or the class ID if applicable) and `startDate`/`endDate` for the current month.
    5. Wrap the method logic in a `try...finally` block. In the `finally` block, call the `LogoutAsync` method with `Method = "logout"` to ensure the session is destroyed, regardless of exceptions.
  </action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Service makes actual data requests, saves JSON strings to database, and explicitly calls logout in finally block.</done>
</task>

</tasks>

<verification>
Ensure that `WebUntisService` contains a `try...finally` structure where `_client.LogoutAsync` is reliably executed. Ensure hardcoded "[]" arrays are gone.
</verification>

<success_criteria>
- IWebUntisClient interface has 6 methods (auth + 4 data + logout).
- WebUntisService pulls live data and serializes it to JSON.
- `logout` is guaranteed to be called via finally block.
- Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-webuntis-integration/02-04-SUMMARY.md`
</output>
