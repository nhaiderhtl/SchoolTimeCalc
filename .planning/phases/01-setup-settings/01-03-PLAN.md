---
phase: 01-setup-settings
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - Components/Pages/Settings.razor
  - Components/Layout/NavMenu.razor
  - Components/Layout/MainLayout.razor
  - Components/App.razor
autonomous: false
requirements: [SET-01]

must_haves:
  truths:
    - "User can view a settings page at /settings"
    - "User can select an Austrian Bundesland from a dropdown or list"
    - "Selection is persisted via the MockAuthService/DbContext"
  artifacts:
    - path: "Components/Pages/Settings.razor"
      provides: "Bundesland selection UI"
      contains: "@page \"/settings\""
    - path: "Components/Layout/NavMenu.razor"
      provides: "Navigation to settings"
      contains: "href=\"settings\""
  key_links:
    - from: "Components/Pages/Settings.razor"
      to: "MockAuthService"
      via: "Dependency Injection"
      pattern: "@inject MockAuthService"
---

<objective>
Build the Settings UI using Tailwind CSS, allowing the mock authenticated user to select and save their Austrian Bundesland.

Purpose: Implement D-04 and D-05 using the Blazor frontend to enforce state selection before holiday calculation.
Output: A styled `/settings` page mapped to the database.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup-settings/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Settings Component and Layout</name>
  <files>Components/Pages/Settings.razor, Components/Layout/NavMenu.razor, Components/Layout/MainLayout.razor, Data/AustrianStates.cs</files>
  <action>
    - Create `Data/AustrianStates.cs` exporting a static list of the 9 Austrian Bundesländer (e.g., "Wien", "Niederösterreich", "Burgenland", "Salzburg", "Steiermark", "Oberösterreich", "Kärnten", "Tirol", "Vorarlberg").
    - Create `Components/Pages/Settings.razor` mapping to `/settings`.
    - Use Tailwind classes (D-01) for a clean, modern form layout with a `<select>` or radio button group for the Bundesland.
    - Inject `MockAuthService` and `ApplicationDbContext` to load the current mock user's Bundesland and save it on `submit`.
    - Update `NavMenu.razor` and `MainLayout.razor` to include a link to Settings and apply a basic sidebar/topbar layout using Tailwind.
    - Force navigation to `/settings` if the mock user's `Bundesland` is null (D-04).
  </action>
  <read_first>
    - Data/ApplicationDbContext.cs
    - Program.cs
  </read_first>
  <acceptance_criteria>
    - `Data/AustrianStates.cs` contains an array or list of 9 states.
    - `Components/Pages/Settings.razor` contains `<select>` or radio buttons rendering states.
    - `Settings.razor` invokes `dbContext.SaveChangesAsync()` on form submission.
    - `dotnet build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>The Settings page is implemented, styled, and saves data to the backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and Functional Verification</name>
  <files></files>
  <action>User to verify Settings UI and state persistence with Tailwind CSS.</action>
  <what-built>Settings UI and state persistence with Tailwind CSS.</what-built>
  <how-to-verify>
    1. Run `dotnet run` (and optionally start postgres using docker).
    2. Navigate to `http://localhost:5000/`.
    3. Verify that the app redirects to `/settings` if no Bundesland is set.
    4. Select a Bundesland ("Wien"), click save, and refresh to verify the selection persists in the mock auth state.
    5. Verify the UI matches a modern Tailwind CSS design.
  </how-to-verify>
  <verify>Manual verification complete</verify>
  <done>User confirmed UI works</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Navigate to the settings route and save a Bundesland.
- Reload the application to verify the saved value loads correctly.
</verification>

<success_criteria>
User successfully selects their Austrian state and the application persists it via Entity Framework to PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-settings/01-03-SUMMARY.md`
</output>