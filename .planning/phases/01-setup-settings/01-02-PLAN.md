---
phase: 01-setup-settings
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Data/ApplicationDbContext.cs
  - Models/ApplicationUser.cs
  - appsettings.json
  - Program.cs
autonomous: true
requirements: [SET-01]

must_haves:
  truths:
    - "A database exists with a User table containing the Bundesland"
    - "Entity Framework Core connects to PostgreSQL"
  artifacts:
    - path: "Data/ApplicationDbContext.cs"
      provides: "Database Context"
      contains: "DbContext"
    - path: "Models/ApplicationUser.cs"
      provides: "User entity"
      contains: "string? Bundesland"
  key_links:
    - from: "Program.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "AddDbContext"
      pattern: "UseNpgsql"
---

<objective>
Configure EF Core, PostgreSQL connection, and initial User model to store the user's selected state (Bundesland) and scaffold a mock authentication state.

Purpose: Implement D-02, D-03, and D-05 to persist the selected Bundesland to the user's profile securely using a database connection.
Output: A configured PostgreSQL DbContext and User table for Bundesland persistence.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup-settings/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entity Framework Core and PostgreSQL Setup</name>
  <files>SchoolTimeCalc.csproj, appsettings.json, Program.cs, Data/ApplicationDbContext.cs, Models/ApplicationUser.cs</files>
  <action>
    - Install `Npgsql.EntityFrameworkCore.PostgreSQL`, `Microsoft.EntityFrameworkCore.Design`, and `Microsoft.EntityFrameworkCore.Tools`.
    - Create `Models/ApplicationUser.cs` containing `public int Id { get; set; }`, `public string Username { get; set; }`, and `public string? Bundesland { get; set; }` (D-05).
    - Create `Data/ApplicationDbContext.cs` inheriting from `DbContext` with a `DbSet<ApplicationUser> Users` property.
    - Add a `DefaultConnection` string pointing to `Host=localhost;Database=schooltimecalc;Username=postgres;Password=postgres` in `appsettings.json` (and `appsettings.Development.json`).
    - Update `Program.cs` to register the `ApplicationDbContext` with `UseNpgsql`.
  </action>
  <read_first>
    - SchoolTimeCalc.csproj
    - Program.cs
  </read_first>
  <acceptance_criteria>
    - `SchoolTimeCalc.csproj` contains `<PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL"`
    - `Data/ApplicationDbContext.cs` inherits from `DbContext`
    - `Program.cs` contains `.AddDbContext<ApplicationDbContext>`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>EF Core and Npgsql are installed, configured, and compile successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations and Mock Authentication State</name>
  <files>Data/ApplicationDbContext.cs, Program.cs, Components/App.razor</files>
  <action>
    - Add a scoped mock authentication service (`MockAuthService`) that registers a dummy user ("testuser") in the database upon startup or login to simulate a WebUntis authenticated state (D-03).
    - Provide a method in the mock service to retrieve the current mock user's profile.
    - Run EF Core migrations to generate the initial schema `dotnet ef migrations add InitialCreate` and optionally auto-migrate on startup for ease of development in `Program.cs` (`dbContext.Database.Migrate()`).
  </action>
  <read_first>
    - Program.cs
    - Data/ApplicationDbContext.cs
  </read_first>
  <acceptance_criteria>
    - `Program.cs` contains `.AddScoped<MockAuthService>()`
    - `Program.cs` calls `dbContext.Database.Migrate()` or similar on startup
    - `MockAuthService` exists and can retrieve a user profile containing `Bundesland`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>Mock auth service is registered and an initial EF Core migration exists.</done>
</task>

</tasks>

<verification>
- `dotnet ef migrations list` shows InitialCreate.
- Code compiles, and EF Core connects to the mock Postgres database (or local connection string).
</verification>

<success_criteria>
The backend data layer is configured to store user settings (Bundesland) alongside a mock authentication system for Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-settings/01-02-SUMMARY.md`
</output>