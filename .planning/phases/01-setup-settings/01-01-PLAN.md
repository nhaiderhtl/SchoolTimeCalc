---
phase: 01-setup-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SchoolTimeCalc.csproj
  - Program.cs
  - tailwind.config.js
  - app.css
  - App.razor
autonomous: true
requirements: [SET-01]

must_haves:
  truths:
    - "Application compiles and runs as a Blazor Web App"
    - "Tailwind CSS classes are applied and render correctly"
  artifacts:
    - path: "SchoolTimeCalc.csproj"
      provides: "Blazor Web App configuration"
      contains: "Microsoft.NET.Sdk.Web"
    - path: "tailwind.config.js"
      provides: "Tailwind CSS config"
  key_links:
    - from: "App.razor"
      to: "app.css"
      via: "Tailwind import"
      pattern: "app.css"
---

<objective>
Scaffold the base Blazor Web App with Tailwind CSS styling.

Purpose: Convert the existing console application into a modern Blazor Web App architecture as decided in D-01, establishing the UI foundation.
Output: A running Blazor application styled with Tailwind.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup-settings/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Blazor Web App</name>
  <files>SchoolTimeCalc.csproj, Program.cs, Components/App.razor, Components/Routes.razor, Components/_Imports.razor, Properties/launchSettings.json</files>
  <action>
    - Delete existing Console app files if necessary, or modify `SchoolTimeCalc.csproj` to use `Microsoft.NET.Sdk.Web` instead of `Microsoft.NET.Sdk`.
    - Scaffold a basic .NET 10 Blazor Web App structure (Interactive Auto mode if possible, or Server mode as default).
    - Update `Program.cs` to map Razor components (`builder.Services.AddRazorComponents()`, `app.MapRazorComponents<App>()`).
    - Create `Components/App.razor` and `Components/Routes.razor` to handle basic routing.
    - Set up `Properties/launchSettings.json` with a dev profile.
  </action>
  <read_first>
    - SchoolTimeCalc.csproj
  </read_first>
  <acceptance_criteria>
    - `SchoolTimeCalc.csproj` contains `Sdk="Microsoft.NET.Sdk.Web"`
    - `Program.cs` contains `builder.Services.AddRazorComponents()`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build</automated>
  </verify>
  <done>Blazor application builds successfully without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Tailwind CSS</name>
  <files>package.json, tailwind.config.js, postcss.config.js, Components/app.css, SchoolTimeCalc.csproj, Components/App.razor</files>
  <action>
    - Implement decision D-01: Set up Tailwind CSS.
    - Initialize npm (`npm init -y`) and install `tailwindcss`, `postcss`, and `autoprefixer`.
    - Create `tailwind.config.js` and configure `content` to scan `**/*.razor` and `**/*.html`.
    - Create `Components/app.css` containing the `@tailwind` directives.
    - Add a build target or MSBuild task in `SchoolTimeCalc.csproj` to automatically compile Tailwind CSS via `npx tailwindcss -i ./Components/app.css -o ./wwwroot/app.css` on build.
    - Link `/app.css` in `Components/App.razor` head.
  </action>
  <read_first>
    - SchoolTimeCalc.csproj
    - Components/App.razor
  </read_first>
  <acceptance_criteria>
    - `tailwind.config.js` exists and contains `content: [**/*.razor]`
    - `SchoolTimeCalc.csproj` contains an MSBuild Target for Tailwind CLI compilation
    - `Components/App.razor` contains `<link href="app.css" rel="stylesheet" />`
  </acceptance_criteria>
  <verify>
    <automated>npm i && dotnet build</automated>
  </verify>
  <done>Tailwind compiles to wwwroot/app.css on build and is referenced in the app.</done>
</task>

</tasks>

<verification>
- `dotnet build` compiles successfully.
- `wwwroot/app.css` is generated and contains compiled Tailwind utility classes.
</verification>

<success_criteria>
The .NET console application has been successfully transformed into a Blazor Web application with Tailwind CSS integration working on build.
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-settings/01-01-SUMMARY.md`
</output>