---
phase: 03-holiday-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SchoolTimeCalc/SchoolTimeCalc.csproj
  - SchoolTimeCalc/Models/Holiday.cs
  - SchoolTimeCalc/Data/ApplicationDbContext.cs
  - SchoolTimeCalc/Services/NationalHolidayService.cs
  - SchoolTimeCalc/Services/SchoolHolidayService.cs
  - SchoolTimeCalc/Program.cs
autonomous: true
requirements: [SYNC-03, SYNC-04]
gap_closure: true

must_haves:
  truths:
    - "System successfully retrieves and caches Austrian national public holidays"
    - "System successfully retrieves state-specific school holidays based on the user's selected Bundesland"
  artifacts:
    - path: "SchoolTimeCalc/Services/NationalHolidayService.cs"
      provides: "National holiday retrieval logic"
    - path: "SchoolTimeCalc/Services/SchoolHolidayService.cs"
      provides: "School holiday retrieval and caching logic"
    - path: "SchoolTimeCalc/Models/Holiday.cs"
      provides: "Database model for storing holidays"
  key_links:
    - from: "SchoolTimeCalc/Services/NationalHolidayService.cs"
      to: "Nager.Date"
      via: "Method call to Nager.Date.DateSystem"
      pattern: "DateSystem\\.GetPublicHolidays"
    - from: "SchoolTimeCalc/Services/SchoolHolidayService.cs"
      to: "SchoolTimeCalc/Data/ApplicationDbContext.cs"
      via: "EF Core db context"
      pattern: "SaveChangesAsync"
---

<objective>
Implement fetching and caching for Austrian national and state-specific school holidays to close verification gaps.

Purpose: Fix missing integrations for Nager.Date and data.gv.at, and setup global caching for school holidays per CONTEXT.md decisions D-01 and D-02.
Output: Database schema updated, NationalHolidayService implemented with Nager.Date, SchoolHolidayService implemented for data.gv.at API.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nico/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-holiday-integration/03-CONTEXT.md
@.planning/phases/03-holiday-integration/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Nager.Date and Database Models</name>
  <files>SchoolTimeCalc/SchoolTimeCalc.csproj, SchoolTimeCalc/Models/Holiday.cs, SchoolTimeCalc/Data/ApplicationDbContext.cs</files>
  <read_first>SchoolTimeCalc/Data/ApplicationDbContext.cs, SchoolTimeCalc/SchoolTimeCalc.csproj</read_first>
  <acceptance_criteria>
    - SchoolTimeCalc.csproj contains `<PackageReference Include="Nager.Date"`
    - Holiday.cs contains `public class Holiday` with properties for Name, Date, Type (National/School), and Bundesland
    - ApplicationDbContext.cs contains `public DbSet<Holiday> Holidays { get; set; }`
  </acceptance_criteria>
  <action>
    Add the `Nager.Date` NuGet package. Create a global `Holiday` entity model in `Models/Holiday.cs` to cache both national and state holidays. Properties should include: `int Id`, `string Name`, `DateTime Date`, `string Type` (e.g. "National", "School"), and `string? Bundesland`. Add `DbSet<Holiday> Holidays` to `ApplicationDbContext.cs`.
  </action>
  <verify>
    <automated>grep -q "Nager.Date" SchoolTimeCalc/SchoolTimeCalc.csproj && grep -q "DbSet<Holiday> Holidays" SchoolTimeCalc/Data/ApplicationDbContext.cs</automated>
  </verify>
  <done>Nager.Date added, Holiday model exists, DbSet added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Holiday Services</name>
  <files>SchoolTimeCalc/Services/NationalHolidayService.cs, SchoolTimeCalc/Services/SchoolHolidayService.cs, SchoolTimeCalc/Program.cs</files>
  <read_first>SchoolTimeCalc/Program.cs</read_first>
  <acceptance_criteria>
    - NationalHolidayService.cs contains `DateSystem.GetPublicHolidays`
    - SchoolHolidayService.cs uses `HttpClient` or `Refit` to fetch from an API (e.g., data.gv.at) and uses `ApplicationDbContext` to save to DB
    - Program.cs contains `builder.Services.AddScoped<NationalHolidayService>()` and `builder.Services.AddScoped<SchoolHolidayService>()`
  </acceptance_criteria>
  <action>
    Create `NationalHolidayService.cs` with a method `GetAustrianHolidays(int year)` that calls `DateSystem.GetPublicHolidays(year, CountryCode.AT)`. Create `SchoolHolidayService.cs` that takes `HttpClient` and `ApplicationDbContext`. It should implement a method to fetch state-specific school holidays (mock the data.gv.at URL if exact endpoint is unknown but structure it as a real HTTP call), catch exceptions (soft fail), and cache results into the `Holidays` DbSet. Register both services in `Program.cs`.
  </action>
  <verify>
    <automated>grep -q "DateSystem.GetPublicHolidays" SchoolTimeCalc/Services/NationalHolidayService.cs && grep -q "AddScoped<SchoolHolidayService>" SchoolTimeCalc/Program.cs</automated>
  </verify>
  <done>Holiday services implemented and registered in DI.</done>
</task>

</tasks>

<verification>
- Nager.Date library is installed and utilized.
- Global cache model for Holidays exists and is wired to EF Core.
- API fetching service for school holidays exists.
</verification>

<success_criteria>
All missing functionality identified in the VERIFICATION gap report is addressed by the new services and models.
</success_criteria>

<output>
After completion, create `.planning/phases/03-holiday-integration/03-02-SUMMARY.md`
</output>
