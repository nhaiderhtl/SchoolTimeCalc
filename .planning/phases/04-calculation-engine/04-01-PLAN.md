---
phase: 04-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SchoolTimeCalc/Models/CalculationResult.cs
  - SchoolTimeCalc/Services/ICalculationService.cs
  - SchoolTimeCalc/Services/CalculationService.cs
  - SchoolTimeCalc/Program.cs
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03, CALC-04]

must_haves:
  truths:
    - "System calculates total remaining school days accurately excluding all holidays and weekends"
    - "System calculates total remaining lessons globally and broken down per subject"
    - "Cancelled lessons are accounted for and excluded from counts"
    - "Dynamic end date is determined based on the last scheduled lesson or holiday"
  artifacts:
    - path: "SchoolTimeCalc/Services/CalculationService.cs"
      provides: "Core calculation logic for remaining time"
      min_lines: 50
    - path: "SchoolTimeCalc/Models/CalculationResult.cs"
      provides: "Data structure for calculation results"
  key_links:
    - from: "SchoolTimeCalc/Services/CalculationService.cs"
      to: "SchoolTimeCalc/Data/ApplicationDbContext.cs"
      via: "Dependency injection"
      pattern: "ApplicationDbContext"
---

<objective>
Implement the Calculation Engine to process timetables and holidays into remaining time metrics.

Purpose: Accurately calculating the exact remaining time (days, hours, and lessons) left in the school year, natively handling differences between Austrian Bundesländer.
Output: Core CalculationService and Result models, fully DI-registered.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calculation-engine/04-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Calculation Models</name>
  <files>SchoolTimeCalc/Models/CalculationResult.cs, SchoolTimeCalc/Models/SubjectRemainingLessons.cs</files>
  <action>Create models to encapsulate the results of the calculation. `CalculationResult` should contain TotalRemainingDays, TotalRemainingLessons, dynamic EndDate (calculated), and a List of `SubjectRemainingLessons` (Subject name/ID, remaining lessons count). Ensure they are standard C# records or classes.</action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Models exist and compile successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Calculation Service</name>
  <files>SchoolTimeCalc/Services/ICalculationService.cs, SchoolTimeCalc/Services/CalculationService.cs, SchoolTimeCalc/Program.cs</files>
  <action>Create an `ICalculationService` interface and its implementation `CalculationService`. Inject `ApplicationDbContext`. The service should contain a method (e.g., `CalculateRemainingTimeAsync(string username)`) that:
1. Queries the DB for the user's timetable from `DateTime.Today` forward.
2. Queries the DB for the user's holidays.
3. Filters out weekends, holidays, and lessons marked as cancelled (`stat` field logic).
4. Computes total days, total lessons, and groups lessons by subject.
5. Determines the dynamic end date (last timetable entry before a long break or simply the last synced timetable entry).
Register the service in `Program.cs` as Transient or Scoped.</action>
  <verify>
    <automated>dotnet build SchoolTimeCalc/SchoolTimeCalc.csproj</automated>
  </verify>
  <done>Service implemented and registered in DI, successfully querying the DbContext.</done>
</task>
</tasks>

<verification>
Check that the project compiles and the calculation logic compiles.
</verification>

<success_criteria>
Models and Service are correctly defined, compile, and DI is configured for the calculation engine.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calculation-engine/04-01-SUMMARY.md`
</output>
