---
phase: 04-calculation-engine
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - SchoolTimeCalc.Tests/Services/CalculationServiceTests.cs
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03, CALC-04]

must_haves:
  truths:
    - "Calculation logic correctly excludes weekends"
    - "Calculation logic correctly excludes holidays"
    - "Calculation logic correctly excludes cancelled lessons"
  artifacts:
    - path: "SchoolTimeCalc.Tests/Services/CalculationServiceTests.cs"
      provides: "Unit tests for Calculation Service"
      min_lines: 50
  key_links:
    - from: "SchoolTimeCalc.Tests/Services/CalculationServiceTests.cs"
      to: "SchoolTimeCalc/Services/CalculationService.cs"
      via: "Dependency injection/Instantiating the service"
      pattern: "CalculationService"
---

<objective>
Implement Unit Tests for the core calculation logic to ensure days, lessons, and holidays are calculated accurately.

Purpose: Guarantee correct behavior of the calculation engine, particularly edge cases like weekends, public holidays, and cancelled classes.
Output: Suite of Unit tests executing correctly.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calculation-engine/04-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Setup Testing Scaffold</name>
  <files>SchoolTimeCalc.Tests/Services/CalculationServiceTests.cs</files>
  <action>Create a new Test class `CalculationServiceTests` using xUnit or the existing test framework. Scaffold a mock `ApplicationDbContext` (using In-Memory DB or mocked DbSet) to return sample timetables and holidays. Add basic test cases for empty timetables and simple 1-day timetables to verify basic plumbing.</action>
  <verify>
    <automated>dotnet test SchoolTimeCalc.Tests/SchoolTimeCalc.Tests.csproj</automated>
  </verify>
  <done>Basic tests compile and run.</done>
</task>

<task type="auto">
  <name>Task 2: Write Comprehensive Calculation Logic Tests</name>
  <files>SchoolTimeCalc.Tests/Services/CalculationServiceTests.cs</files>
  <action>Expand `CalculationServiceTests` to include specific edge cases based on `04-CONTEXT.md`:
1. Weekends: Setup timetable crossing a weekend and assert days are correctly subtracted.
2. Holidays: Setup timetable overlapping a holiday in the mock DB, assert days and lessons are subtracted correctly.
3. Cancelled Lessons (`stat`): Mock a cancelled lesson and assert it doesn't count towards the remaining subject lessons or total lessons count.
4. Dynamic End Date: Mock a timeline that stops at Sommerferien and assert the correct EndDate is returned.</action>
  <verify>
    <automated>dotnet test SchoolTimeCalc.Tests/SchoolTimeCalc.Tests.csproj</automated>
  </verify>
  <done>All tests compile and pass, ensuring accurate metrics.</done>
</task>
</tasks>

<verification>
Check that `dotnet test` executes successfully.
</verification>

<success_criteria>
Comprehensive unit tests cover weekends, holidays, cancelled lessons, and dynamic end dates.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calculation-engine/04-02-SUMMARY.md`
</output>
